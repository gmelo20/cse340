<%- include('partials/header') %>

<form method="POST">

<label>

Category Name

</label>

<input
  type="text"
  name="category_name"
  required
  maxlength="100"
  value="<%= category.category_name %>"
>

<button type="submit">

Update Category

</button>

</form>

<% if (errors.length > 0) { %>

<ul>

<% errors.forEach(error => { %>

<li><%= error %></li>

<% }) %>

</ul>

<% } %>

<%- include('partials/footer') %>